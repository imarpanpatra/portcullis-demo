# portcullis-demo

A deliberately boring repository. It exists so that
[Portcullis](https://github.com/imarpanpatra/portcullis) has somewhere real to open
a pull request when an audit passes and a human approves the write.

Nothing here is meant to be used. The only interesting file is `package.json`,
which is what the agent edits when it admits a dependency.
